{
  "buildCommand": "cd packages/frontend && npm install && npm run build",
  "outputDirectory": "packages/frontend/dist",
  "functions": {
    "api/**/*.ts": {
      "runtime": "@vercel/node"
    }
  }
}