{
  "name": "database",
  "version": "1.0.0",
  "description": "Database schemas, migrations and seeds for QMS Automotive",
  "scripts": {
    "migrate": "prisma migrate deploy",
    "migrate:dev": "prisma migrate dev",
    "generate": "prisma generate",
    "studio": "prisma studio",
    "seed": "tsx seeds/seed.ts",
    "reset": "prisma migrate reset --force"
  },
  "dependencies": {
    "@prisma/client": "^5.7.1"
  },
  "devDependencies": {
    "prisma": "^5.7.1",
    "tsx": "^4.6.2",
    "@shared": "workspace:*"
  }
}